package console_lms.models;

import java.io.Serializable;

/**
 * Defines the role of a user in the Library Management System.
 */
public enum UserRole implements Serializable {
    ADMIN,
    USER
}
